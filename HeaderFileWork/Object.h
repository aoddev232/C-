#pragma once
//pragma once makes sure code is not included more than once! (when working with multiple header files that include each other)
class Object
{
public:
	virtual void BeginPlay();
	void ObjectFunction();
};